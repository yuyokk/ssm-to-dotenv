{
  "name": "@yuyokk/ssm-to-dotenv",
  "version": "0.0.1",
  "license": "MIT",
  "author": "Iurii Kucherov",
  "description": "Fetches environment variables from AWS SSM Parameter Store and writes them to a .env file",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yuyokk/ssm-to-dotenv.git"
  },
  "main": "dist/main.js",
  "scripts": {
    "build": "npx rimraf dist && npx tsc",
    "test": "vitest"
  },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.864.0"
  },
  "devDependencies": {
    "@tsconfig/node24": "^24.0.1",
    "@types/node": "^24.2.1",
    "rimraf": "^6.0.1",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
